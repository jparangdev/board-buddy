spring:
  config:
    activate:
      on-profile: local
  data:
    redis:
      host: localhost
      port: 6379
---
spring:
  config:
    activate:
      on-profile: develop
  data:
    redis:
      host: develop-redis
      port: 6379
---
spring:
  config:
    activate:
      on-profile: production
  data:
    redis:
      host: production-redis
      port: 6379
